//const splittedPath = __dirname.split('/')
//const path = `${splittedPath[splittedPath.length - 2]}/${splittedPath[splittedPath.length - 1]}`
module.exports = {
  //publicPath: process.env.NODE_ENV === 'production' ? `${path}/dist/` : '/'
  publicPath: './'
}
